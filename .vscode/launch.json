{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug NotBlood",
            "type": "cppdbg",
            "request": "launch",
            "linux": {
                "program": "${workspaceRoot}/notblood",
            },
            "windows": {
                "program": "${workspaceRoot}/notblood.exe",
            },
            "cwd": "${workspaceRoot}",
        }
    ]
}